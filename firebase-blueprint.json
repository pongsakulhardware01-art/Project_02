{
  "entities": {
    "Settings": {
      "title": "AppSettings",
      "description": "Application settings including multi-supplier configurations, price lists, costs, weights, custom products, deleted items, and delivery rules.",
      "type": "object",
      "properties": {
        "activeSupplierId": {
          "type": "string",
          "description": "ID of the currently active supplier"
        },
        "suppliers": {
          "type": "array",
          "description": "List of all registered supplier profiles"
        },
        "prices": {
          "type": "object",
          "description": "Base selling prices for all precast concrete products"
        },
        "costs": {
          "type": "object",
          "description": "Base production or supplier costs"
        },
        "weights": {
          "type": "object",
          "description": "Physical unit weights for logistics calculation"
        },
        "customProducts": {
          "type": "array",
          "description": "Custom or customized products created by the user"
        },
        "deletedItemIds": {
          "type": "array",
          "description": "List of deleted product IDs hidden from the catalog and calculations"
        },
        "defaultDestination": {
          "type": "object",
          "description": "Default delivery site destination"
        }
      },
      "required": [
        "activeSupplierId",
        "suppliers",
        "prices",
        "weights"
      ]
    }
  },
  "firestore": {
    "/settings/{documentId}": {
      "schema": "Settings",
      "description": "Shared application configuration and price lists"
    }
  }
}
